import { notFound } from 'next/navigation';
import { getDictionary, isLocale } from '@/lib/i18n';
import { pageMetadata, SOURCE_URL } from '@/lib/seo';
export async function generateMetadata({params}: {params:Promise<{locale:string}>}) { const {locale}=await params; if(!isLocale(locale)) return {}; const d=getDictionary(locale); return pageMetadata(locale,'/about',d.about,d.aboutText); }
export default async function About({params}: {params:Promise<{locale:string}>}) { const {locale}=await params; if(!isLocale(locale)) notFound(); const d=getDictionary(locale); return <article className="content-page prose-page"><header className="page-intro"><span className="eyebrow">Awesome Game Security</span><h1>{d.about}</h1><p>{d.aboutText}</p></header><section><h2>{d.source}</h2><p>{d.collectionScope}</p><p><a href={SOURCE_URL} target="_blank" rel="noopener noreferrer">gmh5225/awesome-game-security ↗</a></p><p>{d.sourceNote}</p><p>{d.classificationNote}</p></section><section><h2>{d.curated}</h2><p>{d.editorialNote}</p></section><section><h2>{d.freshness}</h2><p>{d.freshnessHelp}</p><p>{d.observedNotPublished}</p><p>{d.checkedNotEndorsed}</p></section><section id="privacy"><h2>{d.privacy}</h2><p>{d.privacyText}</p><p>{d.followingLocal}</p></section></article>; }
